- hosts: appServers
  become: true
  tasks:
  - name: Install nginx
    yum:
      name: nginx
      state: latest
  - name: Copy index.html
    copy:
      src: index.html
      dest: /usr/share/nginx/html/index.html  
  - name: Start nginx service
    service:
      name: nginx
      state: started
